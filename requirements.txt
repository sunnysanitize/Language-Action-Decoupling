# Needed by agent files that call a model.
openai>=1.0.0
python-dotenv>=1.0.0

# Needed by the detector and the analysis in experiments/. The simulator and
# the test suite do not import these -- an episode still runs, and
# `python -m unittest discover` still passes, without them installed.
numpy>=1.26
scipy>=1.11
scikit-learn>=1.4
